name: Solitaire Lifecycle Regression

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lifecycle:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
      - name: Install Playwright
        run: npm install --no-save playwright@1.55.0 --no-audit --no-fund
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Start local site
        run: |
          nohup node scripts/static-server.js 4173 >/tmp/mahjong-lifecycle-server.log 2>&1 &
          server_pid=$!
          ready=false
          for i in {1..30}; do
            if ! kill -0 "$server_pid" 2>/dev/null; then
              cat /tmp/mahjong-lifecycle-server.log
              exit 1
            fi
            if curl -fsS http://127.0.0.1:4173/index.html >/dev/null; then
              echo "Static server ready after ${i} attempt(s)."
              ready=true
              break
            fi
            sleep 0.5
          done
          if [ "$ready" != true ]; then
            echo 'Static server did not become ready within 15 seconds.'
            cat /tmp/mahjong-lifecycle-server.log
            exit 1
          fi
          echo "$server_pid" >/tmp/mahjong-lifecycle-server.pid
          echo "Static server kept alive for lifecycle regression (pid ${server_pid})."
      - name: Exercise Solitaire lifecycle
        run: |
          server_pid=$(cat /tmp/mahjong-lifecycle-server.pid)
          trap 'kill "$server_pid" 2>/dev/null || true' EXIT
          node scripts/solitaire-lifecycle-regression.js
      - name: Dump server log on failure
        if: failure()
        run: cat /tmp/mahjong-lifecycle-server.log || true
